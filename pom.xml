<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.j-util</groupId>
    <artifactId>reduction-store-build</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>Reduction Store Build</name>
    <description>Build aggregator for Reduction Store.</description>
    <url>https://github.com/j-util/reduction-store</url>

    <modules>
        <module>reduction-store</module>
        <module>reduction-store-processor</module>
    </modules>

    <properties>
        <central.skipPublishing>true</central.skipPublishing>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.5.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <version>3.1.4</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.4</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.12.0</version>
                <configuration>
                    <failOnWarnings>true</failOnWarnings>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.11.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                            <skipPublishing>${central.skipPublishing}</skipPublishing>
                            <checksums>all</checksums>
                            <excludeArtifacts>
                                <excludeArtifact>reduction-store-build</excludeArtifact>
                            </excludeArtifacts>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>local-central-bundle</id>
            <activation>
                <property>
                    <name>central.skipPublishing</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.2.0</version>
                        <executions>
                            <execution>
                                <id>create-local-central-bundle</id>
                                <phase>deploy</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <property name="bundle.staging"
                                                  value="${project.build.directory}/central-staging"/>
                                        <property name="bundle.output"
                                                  value="${project.build.directory}/central-publishing"/>
                                        <property name="core.bundle.directory"
                                                  value="${bundle.staging}/io/github/j-util/reduction-store/${project.version}"/>
                                        <property name="processor.bundle.directory"
                                                  value="${bundle.staging}/io/github/j-util/reduction-store-processor/${project.version}"/>
                                        <delete dir="${bundle.staging}"/>
                                        <delete dir="${bundle.output}"/>
                                        <mkdir dir="${core.bundle.directory}"/>
                                        <mkdir dir="${processor.bundle.directory}"/>
                                        <mkdir dir="${bundle.output}"/>
                                        <copy file="${project.basedir}/reduction-store/pom.xml"
                                              tofile="${core.bundle.directory}/reduction-store-${project.version}.pom"/>
                                        <copy todir="${core.bundle.directory}">
                                            <fileset dir="${project.basedir}/reduction-store/target">
                                                <include name="reduction-store-${project.version}*.jar"/>
                                                <include name="reduction-store-${project.version}*.asc"/>
                                            </fileset>
                                        </copy>
                                        <copy file="${project.basedir}/reduction-store-processor/pom.xml"
                                              tofile="${processor.bundle.directory}/reduction-store-processor-${project.version}.pom"/>
                                        <copy todir="${processor.bundle.directory}">
                                            <fileset dir="${project.basedir}/reduction-store-processor/target">
                                                <include name="reduction-store-processor-${project.version}*.jar"/>
                                                <include name="reduction-store-processor-${project.version}*.asc"/>
                                            </fileset>
                                        </copy>
                                        <checksum algorithm="MD5" fileext=".md5">
                                            <fileset dir="${bundle.staging}">
                                                <exclude name="**/*.asc"/>
                                            </fileset>
                                        </checksum>
                                        <checksum algorithm="SHA-1" fileext=".sha1">
                                            <fileset dir="${bundle.staging}">
                                                <exclude name="**/*.asc"/>
                                                <exclude name="**/*.md5"/>
                                            </fileset>
                                        </checksum>
                                        <checksum algorithm="SHA-256" fileext=".sha256">
                                            <fileset dir="${bundle.staging}">
                                                <exclude name="**/*.asc"/>
                                                <exclude name="**/*.md5"/>
                                                <exclude name="**/*.sha1"/>
                                            </fileset>
                                        </checksum>
                                        <checksum algorithm="SHA-512" fileext=".sha512">
                                            <fileset dir="${bundle.staging}">
                                                <exclude name="**/*.asc"/>
                                                <exclude name="**/*.md5"/>
                                                <exclude name="**/*.sha1"/>
                                                <exclude name="**/*.sha256"/>
                                            </fileset>
                                        </checksum>
                                        <zip destfile="${bundle.output}/central-bundle.zip"
                                             basedir="${bundle.staging}"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
