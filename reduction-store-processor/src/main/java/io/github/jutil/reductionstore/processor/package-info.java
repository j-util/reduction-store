/**
 * Compiler integration for generating reduction stores.
 *
 * <p>Applications configure the processor artifact on the annotation
 * processor path; they do not call this package directly.
 */
package io.github.jutil.reductionstore.processor;
